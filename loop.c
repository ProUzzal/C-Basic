#include <stdio.h>

int main()
{

    /*
    scanf("%d", &num);

    for (i = 1; i <= 10; i++)
    {
       
        //even Number
        if (i % 2 == 0)
        {
            printf("%d is an even number\n", i);
        }
        //odd
        if (i % 2 != 0)
        {
            printf("%d is an Odd Number\n", i);
        }
        //Sum of all numbers
        sum += i;
        
//table of 1
        printf("%d x %d = %d\n",num,i, num*i);
    }
    printf("Sum of 0 to 100 = %d",sum);
    */

   //Power 
   
    return 0;
}